# class_and_instance.md

- The `File::path` class method is called on the class `File` and will return
  the provided path as a string

- The `File#path` instance method is called on an object of the `File` Class
  and returns the name of the path used to create the file, which may or may not
  be the current path of the file.